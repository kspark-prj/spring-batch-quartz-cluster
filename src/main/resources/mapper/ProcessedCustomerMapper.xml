<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.ProcessedCustomerMapper">

    <insert id="insertProcessedCustomersBulk">
        INSERT INTO processed_customer (
            id,
            name,
            email,
            processed_at,
            api_response
        ) VALUES
        <foreach collection="list" item="element" separator=",">
            (
                #{element.id},
                #{element.name},
                #{element.email},
                #{element.processedAt},
                #{element.apiResponse}
            )
        </foreach>
        ON CONFLICT (id) DO UPDATE SET
            name = EXCLUDED.name,
            email = EXCLUDED.email,
            processed_at = EXCLUDED.processed_at,
            api_response = EXCLUDED.api_response
    </insert>
    <!-- MyBatisBatchItemWriter 전용 단건 Insert/Upsert 쿼리 -->
    <insert id="insertProcessedCustomer" >
        INSERT INTO processed_customer (
            id,
            name,
            email,
            processed_at,
            api_response
        ) VALUES (
            #{id},
            #{name},
            #{email},
            #{processedAt},
            #{apiResponse}
        )
        ON CONFLICT (id) DO UPDATE SET
            name = EXCLUDED.name,
            email = EXCLUDED.email,
            processed_at = EXCLUDED.processed_at,
            api_response = EXCLUDED.api_response
    </insert>


</mapper>
